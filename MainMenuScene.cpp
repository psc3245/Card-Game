#pragma once
#include "Scene.cpp"

class MainMenuScene : Scene {
    public:
        void handleEvent(sf::Event event) {

        }

        void update() {
            
        }

        void draw(sf::RenderWindow& window) {
            
        }
};